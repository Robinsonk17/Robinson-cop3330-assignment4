public class NeededTesting
{
    // addItem(item)
    // -- Test if the items correctly adds


    // removeItem(item)
    // -- Test if items can properly be removed


    // displayList(filter)
    // -- Check that correct list displays when prompted


    // addList(list)
    // -- Check list is properly add

    // removeList(list)
    // -- Test if items can properly be removed

    // saveList()
    // -- Checks if lists are saved correctly

    // loadList(file)
    // -- Checks if list are loaded as instructed

    // Constructor(name)

     // (setName())
    // -- Checks if named is set correctly
    // (getName())
    // (setItem(item, index))
    // (getItem(index))








}
